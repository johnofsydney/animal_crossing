

<%= form_with url: "/animals/results", method: :get do |form| %>
  <%= form.label :query, "Search for name:" %>
  <%= form.text_field :name %>
  <%= form.label :size, "Search for size:" %>

  <%= form.select :size, Animal.sizes, { include_blank: true } %>

  <%= form.submit "Search" %>
<% end %>
